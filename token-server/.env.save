cat .env
sleep 2 && curl http://localhost:3030/health
curl http://localhost:3030/health 2>/dev/null && echo "✅ Server is running!" || echo "❌ Server not responding"
cd token-server
pwd
OPENAI_API_KEY=sk-your-key-here-replace-this
PORT=3030
